type featurePackage = {
  detail: string
}
export type pricingPackage = {
  name: string
  description: string
  isActive?: boolean
  // price:string,
  // usage:string
  // featuresList:featurePackage[]
}
